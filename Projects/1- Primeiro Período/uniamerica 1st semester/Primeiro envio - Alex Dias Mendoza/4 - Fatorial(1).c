#include <stdio.h>
int main() {
    
    int n, total;
    
    printf("Digite o numero a ser fatorado: ");
    scanf("%d", &n);
        if (n >= 1);
        {
            (n >= 1);
            total = n - 1 * n;
            n --;
        };
        
    printf("O fatorial eh: %d", total);
    return 0;

}